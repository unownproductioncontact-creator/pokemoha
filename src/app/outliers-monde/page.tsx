import { ScanPage } from "@/components/ScanPage";

export default function Page() {
  return (
    <ScanPage
      kind="world"
      title="Outliers du monde"
      subtitle="Scan international (US, JP, UK, DE…). Repère les concepts viraux à adapter en FR — jamais à copier."
      note="Régions + mécaniques virales détectées, cache 20 h. La mécanique suggère l'angle réutilisable."
    />
  );
}
